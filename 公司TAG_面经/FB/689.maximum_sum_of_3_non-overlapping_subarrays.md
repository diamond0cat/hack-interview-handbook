- DP

```
用dp[i][j]代表到j元素为止，在有i个子数组的情况下最大的值。之后再回过头找最早得到最大和的位置。相比官解的方法来说应该更直白易懂些。

转移方程的意思是：对于dp[i][j]，其最大值是下面两种情况之一：

要么在j-1个元素的时候已经找到了i个数组的最大值；
要么j-k个元素为止有i-1个数组的最大值，然后加上结束于第j个元素的数组的和形成i个数组的最大值。
```
